import 'dart:ui';

class LocaleConfig {
  static const List<Locale> supportedLocales = [Locale('en')];

  static const fallbackLocale = Locale('en');
  static const localeAssets = 'assets/localization';
}
